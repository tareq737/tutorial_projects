import 'package:flutter/material.dart';

class SearchLocal extends StatelessWidget {
  const SearchLocal({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('hello Tareq!!!')),
    );
  }
}