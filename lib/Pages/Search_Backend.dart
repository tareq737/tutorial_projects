import 'package:flutter/material.dart';

class SearchBackend extends StatelessWidget {
  const SearchBackend({super.key});

  @override
  Widget build(BuildContext context) {
    return const Scaffold(
      body: Center(child: Text('hello Tareq!!!')),
    );
  }
}